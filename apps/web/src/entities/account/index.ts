/**
 * Public API of the `account` entity.
 *
 * Outside callers must import from here, not from `model/*` directly.
 */
export { useAccounts } from './model/use-accounts';
export { useActiveAccountId } from './model/use-active-account';
export type { AccountRecord } from './model/types';
