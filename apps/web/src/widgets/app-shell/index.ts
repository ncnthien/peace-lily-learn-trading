/**
 * Public API of the `app-shell` widget.
 *
 * Only `AppShell` is exported — `AppHeader` and `AppSidebar` are
 * internal collaborators and should not be imported from outside this
 * slice.
 */
export { AppShell } from './ui/AppShell';
