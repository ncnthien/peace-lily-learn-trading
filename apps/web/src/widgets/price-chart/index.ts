/**
 * Public API of the `price-chart` widget.
 *
 * Only the React component is exported; the lightweight-charts
 * primitive (`PositionBoxesPrimitive`) and the chart-view persistence
 * helpers stay internal because other layers don't need them.
 */
export { PriceChart } from './ui/PriceChart';
